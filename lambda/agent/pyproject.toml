[project]
name = "agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "boto3>=1.39.13",
    "pydantic>=2.11.7",
    "strands-agents>=1.2.0",
]

[dependency-groups]
dev = [
    "pyink>=24.10.1",
]
